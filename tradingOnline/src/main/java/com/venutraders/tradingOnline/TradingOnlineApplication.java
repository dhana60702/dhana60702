 package com.venutraders.tradingOnline;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TradingOnlineApplication {/* extends SpringBootServletInitializer */

	public static void main(String[] args) {
		SpringApplication.run(TradingOnlineApplication.class, args);
	}

	/*
	 * @Override protected SpringApplicationBuilder
	 * configure(SpringApplicationBuilder builder) { return
	 * builder.sources(TradingOnlineApplication.class); }
	 */
}
